<div align="center">
    <b>⚠️ <em>This repo is under active development and not ready to be used</em> ⚠️</b>
    <img alt="boba logo" src="./assets/readme/banner.png" width="100%"/>
</div>

`wayne` is an experimental implementation of the wayland server protocol purely in rust. It does not attempt to mirror the `libwayland` api, but rather re-imagine the api in a way that works best with rusty patterns and norms.